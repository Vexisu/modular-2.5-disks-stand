# Modular 2.5 inch disks stand
This project was made in SolveSpace. The repository contains `.slvs` project files and exported `.stl` files.
`Terminator` is required to close a missing wall on the last `module`.
